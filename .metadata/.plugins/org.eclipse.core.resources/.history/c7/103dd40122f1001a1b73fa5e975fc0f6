
#include <stdio.h>
#include "inout.h"
#include "strings_eng.h"

void welcome(void)
{
    printf("\n%s\n", LINE);
    printf("%s\n"  , MSG_WELCOME);
    printf("%s\n\n", LINE);

    return;
}

/*int obtenerValor(void)
{
    int salida;
    scanf("%d", &salida);
    return salida;
}*/

/** \brief Recibe dos valores numéricos enteros
 *
 * \param Valor correspondiente a la variable A
 * \param Valor correspondiente a la variable B
 * \return Imprime a ambos valores por pantalla
 *
 */
void printValues(int a, int b)
{
    printf("\n  A=%d \tB=%d", a, b);
    return;
}

/** \brief Imprime un string
 *
 * \param no recibe ningún parametro
 * \return imprime un string
 *
 */
void printLine(void)
{
    printf("%s\n", LINE);
    return;
}

/** \brief Imprime un string
 *
 * \param no recibe ningún parametro
 * \return imprime un string
 *
 */
void printInputValueA(void)
{
    printf("\n%s\n", MSG_INPUT_VALUE_A);
    return;
}

/** \brief Imprime un string
 *
 * \param no recibe ningún parametro
 * \return imprime un string
 *
 */
void printInputValueB(void)
{
    printf("\n%s\n", MSG_INPUT_VALUE_B);
    return;
}

/** \brief Imprime un string
 *
 * \param no recibe ningún parametro
 * \return imprime un string
 *
 */
void printInputOperation(void)
{
    printf("\n\n\n%s", MSG_INPUT_OPERATION);
    return;
}


/** \brief Recibe un valor numérico y lo imprime con una descripción(string)
 *
 * \param Valor correspondiente al resultado de la suma
 * \return devuelve un string y un valor
 *
 */
void printSum(float resultado)
{
    printf("\n%s%.0f\n", MSG_PRINT_SUM, resultado);
    return;
}

/** \brief Recibe un valor numérico y lo imprime con una descripción(string)
 *
 * \param Valor correspondiente al resultado de la resta
 * \return devuelve un string y un valor
 *
 */
void printSubtraction(float resultado)
{
    printf("\n%s%.0f\n", MSG_PRINT_SUBTRACTION, resultado);
    return;
}

/** \brief Recibe un valor numérico y lo imprime con una descripción(string)
 *
 * \param Valor correspondiente al resultado de la division
 * \return devuelve un string y un valor
 *
 */
void printDivision(float resultado)
{
    printf("\n%s%.2f\n", MSG_PRINT_DIVISION, resultado);
    return;
}

/** \brief Recibe un valor numérico y lo imprime con una descripción(string)
 *
 * \param Valor correspondiente al resultado de la multiplicacion
 * \return devuelve un string y un valor
 *
 */
void printMultiplication(float resultado)
{
    printf("\n%s%.0f\n", MSG_PRINT_MULTIPLICATION, resultado);
    return;
}

/** \brief Recibe un valor numérico y lo imprime con una descripción(string)
 *
 * \param Valor correspondiente al resultado del factorial de A
 * \return devuelve un string y un valor
 *
 */
void printFactorialA(float resultado)
{
    printf("\n%s%.0f\n", MSG_PRINT_FACTORIAL_A, resultado);
    return;
}

/** \brief Recibe un valor numérico y lo imprime con una descripción(string)
 *
 * \param Valor correspondiente al resultado del factorial de B
 * \return devuelve un string y un valor
 *
 */
void printFactorialB(float resultado)
{
    printf("\n%s%.0f\n", MSG_PRINT_FACTORIAL_B, resultado);
    return;
}

/** \brief Imprime un string
 *
 * \param no recibe ningún parametro
 * \return imprime un string
 *
 */
void printGoodbye(void)
{
    printf("%s\n", MSG_GOODBYE);
    return;
}

/** \brief Imprime un string
 *
 * \param no recibe ningún parametro
 * \return imprime un string
 *
 */
void printInvalidOperation(void)
{
    printf("\n%s\n", MSG_INVALID_OPERATION);
    return;
}

/** \brief Imprime un string
 *
 * \param no recibe ningún parametro
 * \return imprime un string
 *
 */
void printErrorDivByCero(void)
{
    printf("\n%s\n", MSG_ERROR_DIVISION_ZERO);
    return;
}

/** \brief Imprime un string
 *
 * \param no recibe ningún parametro
 * \return imprime un string
 *
 */
void printInvalidInput(void)
{
    printf("\n%s\n", MSG_INVALID_CHAR_INPUT);
    return;
}

/** \brief Imprime un string
 *
 * \param no recibe ningún parametro
 * \return imprime un string
 *
 */
void printErrorFactNegative(void)
{
    printf("\n%s\n", MSG_ERROR_FACT_NEGATIVE);
    return;
}

/** \brief Imprime un string
 *
 * \param no recibe ningún parametro
 * \return imprime un string
 *
 */
void printExitConfirm(void)
{
    printf("\n%s\t", MSG_EXIT_CONFIRM);
    return;
}
